//this program provid information about foods cholestrol level
//this program also shows lists of foods 

 #include<iostream>
using namespace std;
/*struct Snacks{
string name;	
int calories;
boolean isAlcohol;
boolean isAdult only;
void displaycategories();	
void Display Item(const const vector<Item>& items,const string&category);
bool canDispenseItem(const Item& item)
{
	cout<<" am good";
	
}
};
struct Drinks{
string name;	
int calories;
boolean isAlcohol;
boolean isAdult only;
void displaycategories();	
void Display Item(const vector<Item>& items,const string&category);
bool canDispenseItem(const Item& item)		
};
struct Dessert{
string name;	
int calories;
bool isAlcohol;
bool isAdult only;
void displaycategories();	
void Display Item(const vector<Item>& items,const string&category);
bool canDispenseItem(const Item& item)		
};

int main(){
	
	int choice;
Snack foods={{" potato,cabaje,tomato,apple,it's cholestrol level is 3%"},
{"bean,atar,shinbura,dube,it's cholestrol level is 1.0%"},{"milk,yoghurt,cheese,it's cholestrol level is 5.0%"},
{"nuts,yalaiwz kibe,cocunut,it's cholestrol level is 70.0%  and  THIS ONE is not allowed for u" }};
cout<<"enter u're a spelling from one(A) up to four(D) to see the lists of foods"<<endl;
	cin>>choice;*/
	int main(){
	bool x;
	const bool sumItem=9;
return 0;	
}
